# URL base de la API (gateway).
# En desarrollo local dejar vacío: el proxy de Vite lo maneja automáticamente.
# En producción (Vercel): poner la URL pública del gateway en Render.
VITE_API_BASE_URL=https://tu-gateway.onrender.com
